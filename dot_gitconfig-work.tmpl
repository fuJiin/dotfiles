# Work identity. Generated by chezmoi from ~/.config/chezmoi/chezmoi.toml.
# Applies to repos under {{ .git.workDir }} via the includeIf in ~/.gitconfig.
# Do not edit directly -- edit the machine's chezmoi.toml and re-apply.
[user]
	name = {{ .git.name }}
	email = {{ .git.workEmail }}
