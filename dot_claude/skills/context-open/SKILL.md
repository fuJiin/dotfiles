# Open Context File

Open the project context file for viewing/editing:

1. If `.agents/CONTEXT.md` doesn't exist, report that and stop.
2. Run `$EDITOR .agents/CONTEXT.md` to open it (or `open .agents/CONTEXT.md` on macOS if no $EDITOR is set).
