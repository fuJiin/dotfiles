{{- if not (and .git.workDir .git.workEmail) }}
# No work identity configured on this machine
.gitconfig-work
{{- end }}

{{- if ne .chezmoi.os "darwin" }}

# macOS-only: Homebrew package list
Brewfile

# macOS-only: desktop GUI terminal
.config/ghostty
.config/ghostty/**
{{- end }}
