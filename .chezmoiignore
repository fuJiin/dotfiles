# Source-only: repo documentation, never a target file in $HOME.
README.md

{{- if not (and .git.workDir .git.workEmail) }}

# No work identity configured on this machine
.gitconfig-work
{{- end }}

{{- if ne .chezmoi.os "darwin" }}

# macOS-only: desktop GUI terminal
.config/ghostty
.config/ghostty/**
{{- end }}
